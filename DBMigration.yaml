services:
  dbapp:
    build:
      context: .
      dockerfile: DockerfileDBAPP
    command: npm run migrate
    ports:
      - "8002:5002"
    volumes:
      - dbapp:/dbapp
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
volumes:
  dbapp: