services:
  fe:
    build:
      context: ../
      dockerfile: DEV/Dockerfile.FE
    command: npm run start
    ports:
      - "8000:3000"
    volumes:
      - ../FE:/FE
    environment:
      - CHOKIDAR_USEPOLLING=true
volumes:
  FE: {}